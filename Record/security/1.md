Spring provide default security using **stater security** dependcy in **pom.xml** and also we can control it further using a class that extends **WebSecurityConfigurerAdapter**

is it correct?

---

## 1️⃣ Default security with `spring-boot-starter-security` ✅ (Correct)

Yes, **this part is correct**.

When you add this dependency in `pom.xml`:

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>
```

Spring Boot **automatically enables security**.

### What happens by default?

* All APIs are **secured**
* **Login form** is auto-generated
* **HTTP Basic authentication** is enabled
* Default username: `user`
* Password is **generated at startup** and printed in logs
* CSRF is enabled (for web apps)

👉 This is called **auto-configuration**.

---

## 2️⃣ Controlling security using `WebSecurityConfigurerAdapter` ❌ (Outdated)

This part is **NO LONGER CORRECT** in modern Spring Security.

### Earlier (Before Spring Security 5.7) ✅

Yes, earlier we used:

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig extends WebSecurityConfigurerAdapter {

    @Override
    protected void configure(HttpSecurity http) throws Exception {
        http
            .authorizeRequests()
            .anyRequest().authenticated()
            .and()
            .httpBasic();
    }
}
```

So your statement **was correct earlier**.

---

## 3️⃣ Current approach (Spring Security 5.7+) ✅ (Interview important)

`WebSecurityConfigurerAdapter` is **deprecated** ❌
**Removed in newer versions**

### ❓ Why removed?

* Too much inheritance
* Hard to customize
* Not functional-programming friendly

---

## 4️⃣ Modern & Correct Way (SecurityFilterChain) ✅

Now we configure security using **beans**, not inheritance.

### Example:

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/public/**").permitAll()
                .anyRequest().authenticated()
            )
            .httpBasic();

        return http.build();
    }
}
```

### Key points:

* No `extends WebSecurityConfigurerAdapter`
* Uses **`SecurityFilterChain`**
* More readable and flexible
* Recommended by Spring team

---

## 5️⃣ Corrected version of your statement ✅

❌ **Your original statement (not fully correct today):**

> we can control it further using a class that extends WebSecurityConfigurerAdapter

✅ **Correct modern statement:**

> In Spring Boot, adding `spring-boot-starter-security` enables default security automatically.
> We can customize security behavior using a `SecurityFilterChain` bean (earlier versions used `WebSecurityConfigurerAdapter`, which is now deprecated).

---

## 6️⃣ streams.One-line interview-ready answer 🧠

> Spring Boot provides default security when `spring-boot-starter-security` is added. Earlier, customization was done using `WebSecurityConfigurerAdapter`, but from Spring Security 5.7 onwards, it is deprecated and replaced by configuration using `SecurityFilterChain`.

---

If you want, I can also explain:

* Why **every request becomes secured**
* How **Spring Security filter chain works internally**
* Difference between **HTTP Basic vs Form Login**
* How to **disable default login page**

Just tell me 👍
