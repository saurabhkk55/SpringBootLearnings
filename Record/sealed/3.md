Excellent question 👍
This is **one of the most common sealed-class confusion points** and **very frequently asked in interviews**.

I’ll explain **step-by-step**, **with rules, code, output, and interview traps**, in very simple words.

---

## 1️⃣ Problem Statement (Your Scenario)

You have:

```java
sealed class X permits A, B {}
```

Now you want:

```java
class D extends B {}
```

👉 **How is this possible?**

---

## 2️⃣ The Golden Rule (VERY IMPORTANT ⭐)

> ❗ **Only the classes listed in `permits` can directly extend a sealed class.**

So:

* `A` and `B` can extend `X`
* `D` **CANNOT** extend `X` directly

But…

---

## 3️⃣ The ONLY Way D Can Extend B

👉 **Class `B` must be `non-sealed`**

This is the key 🔑

---

## 4️⃣ Correct & Legal Solution ✅

### Step 1: Sealed class X

```java
public sealed class X
        permits A, B {
}
```

---

### Step 2: Class A (closed)

```java
public final class A extends X {
}
```

✔ No one can extend `A`

---

### Step 3: Class B (OPEN it deliberately)

```java
public non-sealed class B extends X {
}
```

✔ `non-sealed` re-opens inheritance

---

### Step 4: Class D extends B

```java
public class D extends B {
}
```

✅ **This works perfectly**

---

## 5️⃣ Visual Diagram (Very Helpful 🧠)

```
        X (sealed)
       /          \
   A (final)   B (non-sealed)
                   |
                   |
                D (normal)
```

---

## 6️⃣ What If B is NOT non-sealed? ❌

### Case 1: B is final

```java
public final class B extends X {}
class D extends B {} // ❌ ERROR
```

💥 Error:

```
Cannot inherit from final 'B'
```

---

### Case 2: B is sealed

```java
public sealed class B extends X permits C {}
class D extends B {} // ❌ ERROR
```

💥 Error:

```
Class D is not allowed to extend sealed class B
```

---

## 7️⃣ If B is sealed, how can D extend it? 🤔

👉 Then **B must explicitly permit D**

```java
public sealed class B
        extends X
        permits C, D {
}
```

```java
public final class D extends B {
}
```

✔ Valid again

---

## 8️⃣ Summary of All Valid Possibilities

| B is declared as | Can D extend B? | Condition                   |
| ---------------- | --------------- | --------------------------- |
| `final`          | ❌ No            | Final blocks inheritance    |
| `sealed`         | ✅ Yes           | Only if `D` is in `permits` |
| `non-sealed`     | ✅ Yes           | No restriction              |

---

## 9️⃣ Most Important Interview Line 💯

> **A class can extend a permitted subclass of a sealed class only if that subclass is declared `non-sealed`, or if it is sealed and explicitly permits that class.**

---

## 🔟 Tricky Interview Questions (Quick Fire)

### Q1️⃣ Can D extend X directly?

❌ No, unless D is listed in `permits`

---

### Q2️⃣ Why does Java enforce this?

✔ To **control inheritance strictly and predictably**

---

### Q3️⃣ Why introduce `non-sealed`?

✔ To allow **controlled escape** from sealed hierarchy

---

### Q4️⃣ Can non-sealed class be extended infinitely?

✅ Yes, like a normal class

---

### Q5️⃣ What is the safest design?

✔ Sealed → Final (most common)

---

## 1️⃣1️⃣ Real-Life Analogy 🏦

* `X` = RBI (sealed)
* `B` = Bank (non-sealed)
* `D` = Branch

👉 RBI controls **banks**, but banks control **branches**

---

## 1️⃣2️⃣ Final streams.One-Line Answer (Interview Perfect)

> Class `D` can extend class `B` **only if `B` is declared `non-sealed`**, or if `B` itself is sealed and explicitly permits `D`.

---

If you want:
👉 **More tricky sealed hierarchy scenarios**
👉 **MCQs + compiler error based questions**
👉 **Sealed + pattern matching exhaustiveness**

Just say 👍
